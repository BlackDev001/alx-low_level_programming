#include "main.h"
/*
 * _isupper: checks for uppercase characters
 * Return: 1(for uppercase characters) or 0(for therwise)
 */
int _isupper(int c)
{
	char c;
	if c = 'A';
	{
		_putchar("%c: %d\n", c, _isupper());
		else
		{
			_putchar("%c: %d\n", c, _isupper());
			return (0);
		}
		return (1);
	}
}

