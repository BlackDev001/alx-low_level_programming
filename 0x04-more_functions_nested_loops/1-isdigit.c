#include "main.h"
/**
 * main - Entry point
 * _isdigit - checks for a digit
 * @c: int to be checked
 * Returns: 0(success), 1(otherwise)
 */
int _isdigit(int c)
{
	int char c;
	return (c >= '0' && c <= '9');
}
