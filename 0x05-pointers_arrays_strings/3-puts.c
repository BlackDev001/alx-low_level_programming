#include "main.h"
/**
 * _puts - Function that prints a string followed by \n to stdout
 * @s: string to be printed
 *
 * Return: nothing
 */
void _puts(char *s)
{
	/* inputing string s for printing*/
	_puts("%s");
}
